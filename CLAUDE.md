@AGENTS.md
@REQUIREMENTS.md

# Workflow

- After making any code change, run `npm test` (requires `npm run dev` already running in another terminal) before considering the change complete. If tests fail, fix the underlying issue rather than skipping or weakening the test.
